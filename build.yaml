targets:
  $default:
    builders:
      json_serializable:
        options:
          # Convierte automaticamente camelCase de Dart a snake_case del JSON
          # (ej: fullName <-> full_name). Refleja la convencion de Postgres.
          field_rename: snake
          explicit_to_json: true
